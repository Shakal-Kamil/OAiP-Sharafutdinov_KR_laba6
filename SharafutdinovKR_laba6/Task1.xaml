<Window x:Class="SharafutdinovKR_laba6.Task1"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SharafutdinovKR_laba6"
        mc:Ignorable="d"
        Title="Task1" Height="450" Width="800">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" BorderBrush="Gray" BorderThickness="1" Padding="10" CornerRadius="5" Margin="0,0,0,10">
            <TextBlock Text="Задание: Даны действительные числа x, y. Определить, принадлежит ли точка с координатами (x, y) заштрихованной части плоскости (круг радиусом 1)" 
                   TextWrapping="Wrap" FontSize="14"/>
        </Border>

        <StackPanel Grid.Row="1" Margin="0,10">
            <TextBlock Text="Введите координаты точки:" FontSize="16" FontWeight="Bold" Margin="0,0,0,10"/>

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="10">
                <StackPanel Margin="10">
                    <TextBlock Text="X:" Margin="0,0,5,0"/>
                    <TextBox x:Name="XTextBox" Width="80" Margin="5" Text="0"/>
                </StackPanel>

                <StackPanel Margin="10">
                    <TextBlock Text="Y:" Margin="0,0,5,0"/>
                    <TextBox x:Name="YTextBox" Width="80" Margin="5" Text="0"/>
                </StackPanel>
            </StackPanel>

            <Button x:Name="CheckButton" Content="Проверить" Width="150" Height="30" Margin="10" Click="CheckButton_Click"/>
        </StackPanel>

        <Border Grid.Row="2" BorderBrush="Gray" BorderThickness="1" Padding="10" CornerRadius="5" Margin="0,10">
            <StackPanel>
                <TextBlock Text="Результат:" FontSize="16" FontWeight="Bold" Margin="0,0,0,5"/>
                <TextBlock x:Name="ResultTextBlock" TextWrapping="Wrap" FontSize="14"/>
            </StackPanel>
        </Border>

        <Border Grid.Row="3" BorderBrush="LightGray" BorderThickness="1" Padding="10" CornerRadius="5" Margin="0,10">
            <StackPanel>
                <TextBlock Text="Условие:" FontSize="14" FontWeight="Bold" Margin="0,0,0,5"/>
                <TextBlock Text="x² + y² ≤ 1" FontSize="16" Foreground="Blue"/>
            </StackPanel>
        </Border>
    </Grid>
</Window>
